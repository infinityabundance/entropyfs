//! Linux POSIX/VFS adapter (FUSE). Converts FUSE operations into storage
//! transactions; contains no entropy algorithms (ADR-0002).

#![forbid(unsafe_code)]

// (module populated by the FUSE implementation step)
