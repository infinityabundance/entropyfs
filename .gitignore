/target
Cargo.lock.orig
*.swp
*~
.idea/
.vscode/
# Never commit test stores / mounts
/tmp
/store-test-*/
*.store
*.store.lock
/mnt-test/
# Fuzz artifacts
/fuzz/target
/fuzz/artifacts
/fuzz/corpus
# C ABI smoke test build artifact (12E.14)
/tools/ffi-smoke/smoke
# Evidence staging
/evidence/.staging
# Compiled binaries sealed by the distro court (regenerable from the
# pinned revision in each manifest; never commit ELF artifacts)
/evidence/portability/*/bin/
# Offline docker-lane container images (multi-hundred-MB build artifacts)
/tools/docker/evidence/
